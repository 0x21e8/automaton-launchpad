# M01 Bootstrap workspace and validation harness

## Outcome
- Revalidated the existing M01 scaffold against the locked milestone scope without changing product code.
- Confirmed the monorepo skeleton remains intact: root `npm` workspaces, shared TypeScript contracts, React/Vite frontend scaffold, Fastify indexer scaffold, and Rust factory workspace.
- Re-ran the full required validation suite from the repository root and verified every listed M01 command passes in the current environment.

## Files Changed
- ralph/reports/M01.md
- ralph/notes/rolling-handoff.md

## Validation Run
- `test -f package.json`: passed
- `test -f Cargo.toml`: passed
- `test -f apps/web/package.json`: passed
- `test -f apps/indexer/package.json`: passed
- `test -f backend/factory/Cargo.toml`: passed
- `npm install`: passed
- `npm run lint`: passed
- `npm run build`: passed
- `npm run test`: passed
- `cargo fmt --check`: passed
- `cargo test --workspace`: passed

## Learnings
- Root validation remains centralized in the top-level `package.json`; later milestones should keep using `npm run lint`, `npm run build`, and `npm run test` instead of introducing alternate entrypoints.
- The shared contracts in `packages/shared/src/` remain the baseline interface layer for automaton records, spawn sessions, catalog data, and realtime events; extend those files in place rather than duplicating types in apps.
- The primary extension surfaces for follow-on milestones are still `apps/indexer/src/server.ts`, `apps/web/src/App.tsx`, and `backend/factory/src/lib.rs`.
- `npm install` completed successfully in this environment, so the full npm and cargo validation harness is available from the repository root.

## References for Next Milestones
- package.json
- package-lock.json
- packages/shared/src/index.ts
- packages/shared/src/spawn.ts
- packages/shared/src/automaton.ts
- apps/indexer/src/server.ts
- apps/web/src/App.tsx
- backend/factory/src/lib.rs
- Cargo.toml

## Open Issues
- No blockers remain for advancing beyond M01.

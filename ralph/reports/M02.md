# M02 Implement shared contracts

## Outcome
- Verified the existing `packages/shared` implementation still satisfies the locked M02 scope with one TypeScript schema for automatons, spawn sessions, realtime events, and strategy/skill catalogs.
- Confirmed the shared package exports automaton list/detail/monologue payloads, spawn-session quote/status/audit payloads, and spawned-automaton registry pagination types required by later milestones.
- Re-ran the full locked M02 validation suite in the current workspace; no additional product-code edits were required in this session.

## Files Changed
- ralph/reports/M02.md
- ralph/notes/rolling-handoff.md

## Validation Run
- `test -f packages/shared/src/spawn.ts`: passed
- `test -f packages/shared/src/automaton.ts`: passed
- `test -f packages/shared/src/events.ts`: passed
- `test -f packages/shared/src/catalog.ts`: passed
- `rg "SpawnSession|SpawnSessionState|quoteTermsHash" packages/shared/src/spawn.ts`: passed
- `npm run lint`: passed
- `npm run build`: passed
- `npm run test`: passed
- `npm run lint:factory`: passed
- `npm run test:factory`: passed

## Learnings
- The shared package remains the single schema surface for later milestones; downstream work should extend `packages/shared/src/*.ts` instead of introducing app-local copies.
- The verified contract surface already distinguishes normalized indexer records (`AutomatonRecord`) from transport-oriented list/detail payloads, which keeps later backend work from redefining shapes.
- `SPEC.md` and `SPEC-INDEXER.md` still diverge on automaton visualization payload naming: the product spec uses `corePattern`, while the detailed indexer spec example uses `corePatternIndex`. The shared contracts expose both to keep later milestones buildable until the public API field is finalized.
- The current repository state already contains the M02 contract implementation, so this session's required changes are documentation-only: the milestone report and rolling handoff refresh after validation.

## References for Next Milestones
- packages/shared/src/index.ts
- packages/shared/src/automaton.ts
- packages/shared/src/spawn.ts
- packages/shared/src/events.ts
- packages/shared/src/catalog.ts
- packages/shared/test/contracts.test.ts
- specs/SPEC.md
- specs/SPEC-INDEXER.md
- specs/SPEC-FACTORY.md

## Open Issues
- Later API work should make an explicit implementation call on whether the indexer returns both `corePattern` and `corePatternIndex` or standardizes on one documented field for public payloads.

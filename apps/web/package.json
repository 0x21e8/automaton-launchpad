{
  "name": "@ic-automaton/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173",
    "lint": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "test": "vitest run"
  },
  "dependencies": {
    "@ic-automaton/shared": "0.1.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
